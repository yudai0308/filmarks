- if @user.errors.any?
  #error_explanation
    .alert.alert-error
      フォームの入力内容に #{@user.errors.count}個のエラーがあります。
    %ul
      - @user.errors.full_messages.each do |msg|
        %li
          * #{msg}
