= link_to "review(#{current_user.reviews.length})", review_users_path
= link_to "clip(#{current_user.clips.length})", clip_movies_path
= link_to "fan(#{current_user.members.length})", fan_members_path
= link_to "follower(#{current_user.followers.length})", followers_users_path
= link_to "following(#{current_user.following.length})", following_users_path
